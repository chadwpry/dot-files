# autoload -Uz vcs_info
#
# zstyle ':vcs_info:*' enable git svn
# zstyle ':vcs_info:git:*' formats ' (%b)'
#
# precmd() { vcs_info }
#
# setopt PROMPT_SUBST
#
# PROMPT="%F{green}%*%f %F{blue}%~%f%F{red}%f%F{green}${vcs_info_msg_0_}%F{white}> ";
